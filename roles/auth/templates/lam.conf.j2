server {
    server_name {{ external_subdomains.lam }}.{{ domain_name }};
    location / {
        proxy_pass http://localhost:4000;
    }
    listen 80; #443 ssl;
#    ssl_certificate /docker/certbot/letsencrypt/live/{{ external_subdomains.lam }}.{{ domain_name }}/fullchain.pem;
#    ssl_certificate_key /docker/certbot/letsencrypt/live/{{ external_subdomains.lam }}.{{ domain_name }}/privkey.pem;
#    include /docker/certbot/letsencrypt/options-ssl-nginx.conf;
#    ssl_dhparam /docker/certbot/letsencrypt/ssl-dhparams.pem;
}