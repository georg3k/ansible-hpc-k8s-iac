---
  
- name: CPU cluster nodes setup
  hosts: cpu_nodes
  become: True
  ignore_unreachable: True
  roles:
  - network
  - chrony
  - nas
  - docker
  - auth
  - zabbix
  - ipmi

- name: Compute cluster setup 
  hosts: gpu_nodes
  become: True
  ignore_unreachable: True
  roles:
  - network
  - chrony
  - nas
  - docker
  - auth
  - slurm
  - zabbix
  - ipmi